---
// src/components/en/PricingEn.astro

const plans = [
  {
    num: "01",
    name: "Starter / Landing MVP",
    price: "$500 - $1500",
    description: "Rapid launch for validation, ad campaigns, and quick lead capture.",
    features: [
      "<b>Scope:</b> Landing page or MVP up to 5 screens.",
      "<b>Design:</b> Responsive UI/UX tailored to your brand.",
      "<b>Integrations:</b> Lead routing to Telegram / Email / Sheets.",
      "<b>Infra:</b> SEO optimization, Vercel/Beget deployment.",
      "<b>Timeline:</b> 48 to 96 hours delivery."
    ],
    highlight: false,
    cta: "Launch MVP"
  },
  {
    num: "02",
    name: "Growth / Full Product",
    price: "$3000 - $5000",
    description: "Production-ready SaaS, marketplace, or mobile-first platform.",
    features: [
      "<b>Scope:</b> Multi-page SPA/PWA application (up to 15 screens).",
      "<b>UX/UI:</b> Custom design system and reusable components.",
      "<b>Backend:</b> Supabase DB, Auth, Stripe/Payment gateway.",
      "<b>Analytics:</b> GA4, custom event funnels.",
      "<b>Guarantee:</b> 1 month of bugfixes and support."
    ],
    highlight: true,
    cta: "Start Product"
  },
  {
    num: "03",
    name: "Advanced / AI Integration",
    price: "from $5000",
    description: "Cost reduction and deep artificial intelligence integration.",
    features: [
      "<b>Audit:</b> Full engineering review of business workflows.",
      "<b>AI Integration:</b> LLMs (Gemini/OpenAI) tailored to your data.",
      "<b>Automation:</b> Autonomous n8n background pipelines.",
      "<b>AI Agents:</b> Knowledge-grounded (RAG) Telegram & web bots.",
      "<b>Support:</b> Up to 3 months dedicated warranty."
    ],
    highlight: false,
    cta: "Discuss AI"
  }
];
---

<section id="pricing" class="py-28 px-6 bg-background relative overflow-hidden">
  <div class="max-w-7xl mx-auto relative z-10">
    <div class="text-center mb-20">
      <div class="mono-tag text-emerald-400 mb-3 font-semibold">[04] TRANSPARENT PRICING</div>
      <h2 class="text-4xl md:text-6xl font-extrabold neo-heading uppercase text-white tracking-tight mb-4">
        TRANSPARENT <span class="text-gradient-emerald">PRICING</span>
      </h2>
      <p class="text-slate-400 text-base max-w-xl mx-auto font-normal">
        Fixed price quotes upfront. Zero hidden fees or sudden rate jumps.
      </p>
    </div>

    <div class="grid grid-cols-1 md:grid-cols-3 gap-8 items-stretch">
      {plans.map((plan, index) => (
        <div 
          class:list={[
            "reveal neo-card p-8 flex flex-col relative transition-all duration-300",
            { "border-emerald-500/60 bg-[#18181c] shadow-[0_0_40px_rgba(16,185,129,0.2)] md:-translate-y-2": plan.highlight }
          ]}
          style={`transition-delay: ${index * 120}ms`}
        >
          {plan.highlight && (
            <div class="absolute -top-3.5 left-1/2 -translate-x-1/2 bg-emerald-500 text-slate-950 font-mono text-[10px] font-extrabold uppercase tracking-widest px-4 py-1 rounded-full shadow-lg">
              MOST POPULAR
            </div>
          )}
          
          <div class="mb-8">
            <div class="flex items-center justify-between mb-2">
              <span class="mono-tag text-emerald-400 font-bold">{plan.num} // PLAN</span>
            </div>
            <h3 class="text-2xl font-extrabold neo-heading text-white uppercase mb-4">{plan.name}</h3>
            <div class="text-3xl md:text-4xl font-extrabold font-mono text-white mb-4 tracking-tight">{plan.price}</div>
            <p class="text-slate-400 text-xs leading-relaxed font-normal">{plan.description}</p>
          </div>

          <ul class="space-y-3.5 mb-10 flex-1 border-t border-white/10 pt-6">
            {plan.features.map((feature) => (
              <li class="flex items-start gap-3 text-xs text-slate-300 font-normal">
                <span class="text-emerald-400 font-bold shrink-0">✓</span>
                <span set:html={feature} />
              </li>
            ))}
          </ul>

          <a 
            href="#contact" 
            class:list={[
              "w-full py-3.5 text-xs uppercase tracking-wider font-bold rounded-lg text-center transition-all active:scale-95",
              plan.highlight 
                ? "btn-emerald" 
                : "btn-outline-neo"
            ]}
          >
            {plan.cta} →
          </a>
        </div>
      ))}
    </div>

    <div class="mt-14 text-center text-xs font-mono text-slate-500 max-w-2xl mx-auto">
      * All milestone scopes agreed before kick-off. Clear progress reporting and ownership.
    </div>
  </div>
</section>
